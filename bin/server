#!/usr/bin/env ruby
# encoding: utf-8

$: << File.expand_path('../../lib', __FILE__)

require 'aws'
require 'json'
require 'disco'
require 'disco/server'


server = Disco.create(username: 'burt').server

$stderr.puts("Server running on #{server.uri}")